# ClearFrame

ClearFrame is a small browser demo that removes passersby from a webcam feed.

It uses plain HTML and JavaScript with TensorFlow.js and BodyPix in the browser.

```
npm install
npm run dev
```
